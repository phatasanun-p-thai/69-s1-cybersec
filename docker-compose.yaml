services:
  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: 69-s1-admin
    environment:
      PGADMIN_DEFAULT_EMAIL: phatasanun-p@rmutp.ac.th
      PGADMIN_DEFAULT_PASSWORD: phatsanun@0969272021
    ports:
      - "8081:80"
    volumes:
      - ./data/pgadmin:/var/lib/pgadmin
